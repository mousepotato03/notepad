import 'package:flutter/material.dart';

class NotepadCover extends StatelessWidget {
  const NotepadCover({super.key});

  @override
  Widget build(BuildContext context) {
    return const ColoredBox(color: Color(0xed93602a));
  }
}
